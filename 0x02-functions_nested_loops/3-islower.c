#include "main.h"
/**
 * _islower - checks if char is lowercase
 *
 * Return: 1 if char is lowercase, otherwise 0
 */

int _islower(int c)
{
int alph = 'c';

if (alph >= 97 && alph <= 122)
{
return (1);
}

else
{
return (0);
}

return (0);
}


