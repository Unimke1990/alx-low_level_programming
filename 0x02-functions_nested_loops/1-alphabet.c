#include "main.h"
/**
 * main - starting point of the program
 *
 * Return: always (0) on success
 */

int main(void)
{
int i;
for (i = 'a'; i >= 97 && i <= 122; i++)
{
_putchar(i);
}
_putchar('\n');

return (0);
}
